# 店铺商品列表页面重构计划

## 一、计划概述

参考 `sp_Product_list.vue` 的样式，重构 `shop-prods.vue` 商品列表页面。保持后端数据获取逻辑不变，仅修改样式和界面布局。

## 二、参考样式分析

### 2.1 参考文件特点
- **搜索组件**：顶部搜索框
- **分类选择**：横向滚动分类列表，带下划线指示器
- **筛选排序栏**：综合、销量、价格升、价格降筛选按钮
- **商品网格**：双列卡片布局
- **商品卡片**：图片 + 标签 + 名称 + 价格 + 销量 + 加购按钮
- **悬浮购物车**：右下角悬浮按钮

### 2.2 核心样式元素
- 背景色：`#f5f5f5`
- 主题色：`#ff4d4f`（红色）
- 卡片圆角：`16rpx`
- 分类下划线指示器
- 价格删除线样式

## 三、重构步骤

### 步骤 1：重构模板结构

**修改内容**：
1. 移除原有的 `header` 区域（店铺信息和背景图）
2. 添加分类选择滚动条
3. 添加筛选排序栏（综合、销量、价格升、价格降）
4. 添加商品数量显示
5. 重构商品列表为网格布局（2列）
6. 添加商品卡片样式（图片、标签、名称、价格、销量、加购按钮）
7. 添加悬浮购物车入口
8. 移除底部的 `shop-tabbar` 和 `diy-tabbar`

### 步骤 2：添加分类和筛选功能

**修改 data 部分**：
```javascript
const Data = reactive({
  // 现有字段保留...
  categories: [],                      // 新增：分类列表
  currentCategoryId: 0,                 // 新增：当前分类ID
  filters: [                            // 新增：筛选选项
    { id: 0, name: '综合' },
    { id: 1, name: '销量' },
    { id: 2, name: '价格升' },
    { id: 3, name: '价格降' }
  ],
  currentFilterId: 0,                  // 新增：当前筛选ID
})
```

**新增方法**：
- `loadCategories()` - 加载分类列表
- `onCategoryChange(categoryId)` - 切换分类
- `onFilterChange(filterId)` - 切换筛选
- `applyFilters()` - 应用筛选排序

### 步骤 3：重构样式（CSS）

**新增样式**：
- 页面容器和背景
- 分类滚动条样式
- 分类项和激活状态
- 筛选栏样式
- 筛选项和激活状态
- 商品数量显示
- 商品网格布局
- 商品卡片样式
- 商品图片和标签
- 商品信息区
- 价格和原价
- 销量和加购按钮
- 加载状态、空状态、没有更多
- 悬浮购物车入口

### 步骤 4：调整数据获取逻辑

**修改 `getProd` 方法**：
- 保持 `http.request` 调用不变
- 添加筛选参数传递
- 保留分页逻辑

## 四、模板结构调整

### 4.1 移除的内容
- 原有 `header` 区域（店铺背景、logo、名称）
- `shop-tabbar` 底部导航
- `diy-tabbar` 自定义导航

### 4.2 新增的模板结构
```vue
<template>
  <view class="shop-prods-page">
    <!-- 分类选择 -->
    <scroll-view class="category-scroll">
      <view class="category-list">
        <!-- 分类项 -->
      </view>
    </scroll-view>

    <!-- 筛选排序栏 -->
    <view class="filter-bar">
      <!-- 筛选选项 -->
      <!-- 商品数量 -->
    </view>

    <!-- 商品列表 -->
    <scroll-view class="product-scroll">
      <view class="product-grid">
        <view class="product-card">
          <!-- 商品卡片 -->
        </view>
      </view>
    </scroll-view>

    <!-- 悬浮购物车 -->
    <view class="cart-tab">
      <!-- 购物车图标和数量 -->
    </view>
  </view>
</template>
```

## 五、数据绑定调整

### 5.1 商品数据映射
```javascript
// 将后端返回的数据映射为前端需要的格式
const product = {
  id: item.spuId,
  name: item.spuName,
  image: item.mainImgUrl,
  price: price(item.priceFee),
  originalPrice: item.marketPriceFee ? price(item.marketPriceFee) : '',
  sales: item.totalSales || 0,
  isNew: item.isNew === 1,
  isHot: item.isHot === 1
}
```

### 5.2 筛选排序逻辑
```javascript
function applyFilters(products) {
  if (currentFilterId === 1) {
    // 按销量排序
    return products.sort((a, b) => b.sales - a.sales)
  } else if (currentFilterId === 2) {
    // 按价格升序
    return products.sort((a, b) => a.price - b.price)
  } else if (currentFilterId === 3) {
    // 按价格降序
    return products.sort((a, b) => b.price - a.price)
  }
  return products
}
```

## 六、样式参考

### 6.1 颜色变量
```css
--bg-color: #f5f5f5;
--theme-color: #ff4d4f;
--text-primary: #333;
--text-secondary: #666;
--text-gray: #999;
--border-color: #eee;
--white: #fff;
```

### 6.2 间距规范
```css
--padding-base: 20rpx;
--padding-lg: 30rpx;
--margin-sm: 16rpx;
--margin-base: 20rpx;
--border-radius: 16rpx;
```

## 七、注意事项

1. **保持数据获取逻辑不变**：继续使用 `http.request` 调用 `/mall4cloud_search/ua/search/simple_page` 接口
2. **兼容性处理**：保留原有 `isLineProds` 切换逻辑（或移除）
3. **响应式适配**：确保在不同屏幕尺寸下显示正常
4. **性能优化**：使用 `lazy-load` 懒加载图片
5. **状态管理**：正确管理 `loading`、`hasMore` 状态

## 八、实施顺序

1. 重构模板结构
2. 添加分类和筛选数据结构
3. 添加分类加载和切换方法
4. 重构商品列表渲染
5. 完善样式（CSS）
6. 测试验证功能
