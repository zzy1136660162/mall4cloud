server:
  port: 9109
spring:
  application:
    name: mall4cloud-biz
  config:
    import:
      - nacos:application.${spring.cloud.nacos.config.file-extension}?refreshEnabled=true
      - nacos:${spring.application.name}.${spring.cloud.nacos.config.file-extension}?refreshEnabled=true
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:101.126.90.255}:${NACOS_PORT:8848}
        username: nacos
        password: Gesoft9919.
        namespace: zzy-dev
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yml
        username: ${spring.cloud.nacos.discovery.username}
        password: ${spring.cloud.nacos.discovery.password}
        namespace: zzy-dev

